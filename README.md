Project for simple GPE analysis
-------------------------------

* 'source init.sh' to initialize the environment and activate it.
* 'source init.sh -c' to clean the project directory and deactivate the
virtual environment.
